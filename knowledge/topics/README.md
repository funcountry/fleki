# Topics

This checked-in directory mirrors the primary semantic pages as reference content.

The live topic pages live under `<resolved_data_root>/topics/`.

Page kinds in v1:

- `topic`
- `playbook`
- `decision`
- `glossary`

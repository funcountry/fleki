# Indexes

This checked-in directory mirrors browse-oriented indexes as reference content.

The live indexes live under `<resolved_data_root>/topics/indexes/`.

These indexes are support artifacts only. `knowledge search` and `knowledge trace` must not depend on them for correctness.

This directory holds human-readable semantic indexes such as:

- by-topic
- recent changes
- unresolved questions

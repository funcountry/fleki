# Review Wiki Quartz Overlay

This directory is the repo-owned Quartz overlay for Fleki's local review wiki.

The installer copies these files into `<state_root>/review-wiki/quartz/`, runs
`npm install`, and keeps the exported markdown content under `content/`.
